[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "cascade-integration-runtime"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = ".."}
packages = ["runtime"]
