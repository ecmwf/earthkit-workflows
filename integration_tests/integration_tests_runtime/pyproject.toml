[build-system]
requires = ["setuptools>=80", "packaging>=25"]
build-backend = "setuptools.build_meta"

[project]
name = "integration_tests_runtime"
version = "0.1.0"
requires-python = ">=3.11"

# [tool.setuptools]
# package-dir = {"" = ".."}
# packages = ["runtime"]

# [tool.setuptools.packages.find]
# include = ["runtime"]
# where = ["src"]

[dependency-groups]
dev = ["build"]
