"""
This module describes and implements the controller, that is, the component
that receives a schedule and manages its execution on one or more remote
workers-executors
"""

# TODO split this module into three(+) submodules:
# - controller
# - executor implementations (possibly external / conditional)
